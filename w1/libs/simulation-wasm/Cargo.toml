[package]
name = "simulation-wasm"
version = "0.1.0"
edition = "2021"

# necessary for wasm final artifacts
[lib]
crate-type = ["cdylib"]

[dependencies]
wasm-bindgen = "0.2"
getrandom = { version = "0.2", features = ["js"] }

simulation = { path = "../simulation" }
